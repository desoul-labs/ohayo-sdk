export type {
  AuthenticationConfig,
  EncryptionConfig,
  GetProvider,
  GetSigner,
  IBindings,
  ICipher,
  IEncryptionProvider,
  ILogger,
  LensConfig,
} from '@lens-protocol/react/dist/declarations/src/config';
